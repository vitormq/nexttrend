# requirements.txt - dependências do PAINEL NextTrend (deploy no Streamlit Cloud)
# Versões idênticas às usadas no treino, para os modelos .pkl carregarem certo.
# As dependências completas do pipeline/API estão em requirements-full.txt
# (usado pelo Docker). Sugestão no Streamlit Cloud: Python 3.11.

streamlit==1.35.0
pandas==2.2.2
numpy==1.26.4
scipy==1.13.0
scikit-learn==1.5.0
xgboost==2.0.3
shap==0.45.0
prophet==1.3.0
plotly==5.22.0
pyarrow==15.0.2
joblib==1.4.2
